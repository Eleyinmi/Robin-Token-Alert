[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "robin-token-alert"
version = "0.1.0"
requires-python = ">=3.12"

[tool.vercel]
entrypoint = "app.py"
